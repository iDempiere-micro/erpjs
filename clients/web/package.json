{
  "name": "erpjs-web-client",
  "version": "1.0.0",
  "description": "EolERP micro frontends web application",
  "main": "index.js",
  "scripts": {
    "installAll": "node process.js \"yarn\"",
    "admin": "(cd ./admin && yarn dev)",
    "base-app": "(cd ./base-app && yarn dev)",
    "start": "npm-run-all -p admin base-app"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "await-exec": "^0.1.2"
  }
}
